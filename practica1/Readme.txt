Actividad 1.4
Los archivos no se pueden ejectuar porque no tienen al main al contrario de el archivo UsoReloj que este si tiene main por lo tanto si se puede ejectuar en la terminal.

Actividad 1.7
Al querer ejectuar el comando  me marca un error
el cual dice que no se encuentra la clase principal, causado por java.lang.ClassNotFoundException

Al realizar el comando java.icc.entrada.Entrada arg1 agr2 arg3
le asigna al Argumento 0: arg1, al Argumento 1: arg2 y al Argumento 2:arg3

Actividad 1.8
Asigna el Argumento 0 a los diferentes parametros que se le pasen a la hora de
correr el programa.

Actividad 1.10
Ambos build realizan las instrucciones target name, mikdir, javac, classpath, java, pathelement, classpath, delete y fileset.

El archivo build en Entrada tiene instrucciones adicionales como property name,
la cual apunta a nombre de la clase, incluye su localización, en la instrucción
target name asigna un "default" la cual esta es seguida de la instrucción depends que asigna a una instrucción de clean, compile y dist, esta instrucción aparece
dos veces.

En el otro archivo build se vuelve a encontrar la instrucción target name(2 veces) la primera tiene una instruccion de compilar y la segunda de generar una documentación.

